# 环境配置：串口

使用：ESP-IDF v5.5.4



### 串口驱动：CP2102

这块 ESP-32F 开发板资料中标注的是 **CP2102 USB 转 TTL**。如果你插上开发板后执行：

```
ls /dev/cu.*
```

能看到类似下面的端口，就不需要额外处理：

```
/dev/cu.usbserial-142101
```

如果看不到串口，需要安装 Silicon Labs 的 **CP210x USB to UART Bridge VCP Driver**。
